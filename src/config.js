export const URL = "https://cartoes1.factafinanceira.com.br";
export const URL_UNICO = "https://cartoes1.factafinanceira.com.br/API3";


export const isHoraMaior = (dataPrimeiraTela) => {
    return false;
    /*
    let dataAtual = new Date()
    dataPrimeiraTela = new Date(dataPrimeiraTela);

    if(dataAtual.getDate() === dataPrimeiraTela.getDate()) {
        let valid = (dataAtual.getHours() > dataPrimeiraTela.getHours()) ? true : false;
        return valid;
    }

    return true;
    */
}
